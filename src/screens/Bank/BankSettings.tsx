import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const BankSettings = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: 'tomato',
      }}>
      <Text>BankSettings</Text>
    </View>
  );
};

export default BankSettings;

const styles = StyleSheet.create({});
