import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const BankWallet = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: 'gold',
      }}>
      <Text>BankWallet</Text>
    </View>
  );
};

export default BankWallet;

const styles = StyleSheet.create({});
