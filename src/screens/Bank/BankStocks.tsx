import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const BankStocks = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: 'cyan',
      }}>
      <Text>BankStocks</Text>
    </View>
  );
};

export default BankStocks;

const styles = StyleSheet.create({});
